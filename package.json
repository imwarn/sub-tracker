{
  "name": "sub-tracker",
  "version": "1.0.0",
  "description": "eSIM 保号 & 订阅费用管理看板 - Cloudflare Workers + KV",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "build": "node scripts/build.js"
  },
  "keywords": [
    "esim",
    "subscription",
    "cloudflare-workers",
    "tracker"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "wrangler": "^4.96.0"
  }
}
