{
  "name": "sub-tracker",
  "version": "1.0.0",
  "description": "eSIM 保号 & 订阅费用管理看板 - Cloudflare Workers + KV",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "node scripts/build.js"
  },
  "keywords": ["esim", "subscription", "cloudflare-workers", "tracker"],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
